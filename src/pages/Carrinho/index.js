import React, { useRef } from 'react'
import { View, Text, FlatList } from 'react-native'

const Carrinho = () => {

    return (
        <View>
            <Text> Carrinho !!!</Text>
        </View>
    )
}

export default Carrinho;